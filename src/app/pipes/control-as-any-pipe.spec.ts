import { ControlAsAnyPipe } from './control-as-any-pipe';

describe('ControlAsAnyPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlAsAnyPipe();
    expect(pipe).toBeTruthy();
  });
});
