import { GroupRoyaltiesPipe } from './group-royalties-pipe';

describe('GroupRoyaltiesPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupRoyaltiesPipe();
    expect(pipe).toBeTruthy();
  });
});
