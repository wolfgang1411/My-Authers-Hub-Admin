import { IsbnFormatPipe } from './isbn-format-pipe';

describe('IsbnFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IsbnFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
