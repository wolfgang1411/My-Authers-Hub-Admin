import { DragNDrop } from './drag-n-drop';

describe('DragNDrop', () => {
  it('should create an instance', () => {
    const directive = new DragNDrop();
    expect(directive).toBeTruthy();
  });
});
