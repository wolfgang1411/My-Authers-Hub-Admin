import { IsbnFormat } from './isbn-format';

describe('IsbnFormat', () => {
  it('should create an instance', () => {
    const directive = new IsbnFormat();
    expect(directive).toBeTruthy();
  });
});
