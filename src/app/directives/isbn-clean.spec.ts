import { IsbnClean } from './isbn-clean';

describe('IsbnClean', () => {
  it('should create an instance', () => {
    const directive = new IsbnClean();
    expect(directive).toBeTruthy();
  });
});
